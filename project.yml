name: Selenia
options:
  bundleIdPrefix: pink.ely
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    # Signing — team "Elysia Muñoz" (a Team ID is not secret; it ships in every app).
    DEVELOPMENT_TEAM: "52K336H235"
    CODE_SIGN_STYLE: Automatic

packages:
  CelestialCore:
    path: ../AstroPackages/CelestialCore
  Astrology:
    path: ../AstroPackages/Astrology

targets:
  Selenia:
    type: application
    platform: iOS
    sources:
      - path: App
    dependencies:
      - package: CelestialCore
        product: CelestialCore
      - package: Astrology
        product: Astrology
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: pink.ely.selenia
        TARGETED_DEVICE_FAMILY: "1,2"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: Selenia
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIStatusBarStyle: UIStatusBarStyleLightContent
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: UIInterfaceOrientationPortrait
        # iPad multitasking requires all four orientations; App Store validation rejects the bundle otherwise.
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_NSLocationWhenInUseUsageDescription: "Selenia uses your location to compute the live chart of the sky above you."
